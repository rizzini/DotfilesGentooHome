--- prepare	2022-10-16 07:54:20.326729171 -0300
+++ prepare	2022-10-16 08:44:10.016266459 -0300
@@ -99,5 +99,10 @@
 _disable() {
-  for _config_name in "$@"; do
+  if [[ "$(pwd)" == *"arch"* ]];then
+    cd ..
     scripts/config -k --disable "${_config_name}"
-  done
+  else
+    for _config_name in "$@"; do
+      scripts/config -k --disable "${_config_name}"
+    done
+  fi
 }
@@ -1644,2 +1649,6 @@
 
+  cd arch
+  ln -s x86 amd64
+  msg2 'PATCH OK'
+
   # modprobed-db
